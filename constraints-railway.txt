# Force langchain-arcade 1.3.1 (ToolManager); newer versions change/remove the API.
langchain-arcade==1.3.1
# Docling: required for RAG document ingest on Railway (PDF/DOCX parsing). Pin 2.x for stable builds.
docling>=2.0,<3
