# Copy to .env and fill. See README and PERSONAL_ASSISTANT_PATTERNS.md §5, C.13.
# Railway: set these in the dashboard or run ./scripts/set_railway_vars.sh (skips RAILWAY_TOKEN).

# Arcade (Gmail, Google Calendar)
ARCADE_API_KEY=
ARCADE_BASE_URL=https://api.arcade.dev
DEFAULT_TIMEZONE=

# LLM: Groq or DeepSeek
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
DEEPSEEK_API_KEY=
LLM_MODEL=deepseek-chat

# User identity (Arcade auth + single-user)
EMAIL=
USER_ID=
# Optional: timezone for time-of-day greeting (e.g. Africa/Windhoek). Default UTC.
TIMEZONE=

# Neon (project management + RAG)
DATABASE_URL=
AUTH_URL=
JWKS_URL=

# Qdrant (long-term memory)
QDRANT_URL=
QDRANT_API_KEY=

# Telegram (single user)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Optional: random string (letters, numbers, hyphen, underscore only; not the URL)
TELEGRAM_WEBHOOK_SECRET=
BASE_URL=

# Railway CLI only (local .env; do not set on Railway service)
# RAILWAY_TOKEN=

# Optional: email monitoring
EMAIL_MONITOR_INTERVAL_MINUTES=10
EMAIL_URGENT_KEYWORDS=urgent,ASAP,deadline
